# Convex URL injected by `npx convex dev` locally and by deployment in CI.
PUBLIC_CONVEX_URL=

# Convex Auth providers
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_RESEND_KEY=
AUTH_EMAIL_FROM="Convex Cards <noreply@example.com>"

# SvelteKit app URL used in local auth redirects
SITE_URL=http://localhost:5173
